formaCard.js

import React, {Component} from "react";


export default class Desing extends Component{
    state={
        title: '',
        description: '',
        cards: [{
            title: "",
            desc: ""
        }]
    }

    onSubmit=(evento)=>{
        //pitnar en consola
        //console.log('hola')
        //comando para no refrescar la pantalla
        evento.preventDefault();
        //datos
        console.log(this.state);

    }

    onChange=(evento)=>{
        console.log(evento.target.name, evento.target.value)
        this.setState({
            [evento.target.name]: evento.target.value
        });
    }

    render() {
        return(
            //componente que pitna el formulario
            <div id="content">
                <form onSubmit={this.onSubmit}>
                    <h2>Titulo</h2> <input type="text" onChange={this.onChange} name="title" value={this.state.title}/>
                    <h2>Descripcion</h2> <input type="text" onChange={this.onChange} name="description" value={this.state.description}/>
                    <br/>
                    <button type="submit">Card</button>
                </form>
            </div>
        )
    }
}